node_modules/
dist/
.next/
coverage/
*.tsbuildinfo
.env
.env.local
.env.*.local
.pglite/
.turbo/

# Regenerated by Next on every run, and differently between `dev` and `build`. Tracking
# them means the repository is dirty the moment anyone starts the app — and a `git pull`
# then fails on files nobody edited, which is a confusing wall to hit on day one.
apps/web/next-env.d.ts
apps/web/AGENTS.md
apps/web/CLAUDE.md
